"""Stateless helpers for the multi-link cartpole. Every function takes `env` first.

`generate_cartpole` is deliberately free of torch and Isaac imports so it can be
unit-tested without booting Kit.
"""
